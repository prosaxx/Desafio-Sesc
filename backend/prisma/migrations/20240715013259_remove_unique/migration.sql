BEGIN TRY

BEGIN TRAN;

-- DropIndex
ALTER TABLE [dbo].[professor] DROP CONSTRAINT [professor_nome_key];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
